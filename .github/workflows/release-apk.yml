# 推送 tag时自动构建 APK 并发布到 GitHub Release
name: Build and Release APK

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Release APK (unsigned)
        run: ./gradlew :demo:assembleRelease --no-daemon

      - name: Get tag version and rename APK
        id: tag
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          cp demo/build/outputs/apk/release/demo-release-unsigned.apk "MyPanorama-$VERSION.apk"

      - name: Upload APK to Release
        uses: softprops/action-gh-release@v2
        with:
          files: MyPanorama-${{ steps.tag.outputs.VERSION }}.apk
          draft: false
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
